# Generated inventory output
composer_dags.csv
*.csv
# ...but keep the checked-in input CSV(s) the pipelines read
!view-authorizations/views.csv

# Credentials — never commit
*.json.key
gcp-key.json
service-account*.json

# Python
__pycache__/
*.pyc
.venv/
venv/

# Local editor / assistant tooling
.claude/
